import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  progress: {
    position: 'relative',
    marginTop: '100px',
  },

  spinner: {
    marginLeft: '48%',
  },
});

export default useStyles;
